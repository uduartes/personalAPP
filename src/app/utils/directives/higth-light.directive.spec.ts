import { HigthLightDirective } from './higth-light.directive';

describe('HigthLightDirective', () => {
  it('should create an instance', () => {
    const directive = new HigthLightDirective();
    expect(directive).toBeTruthy();
  });
});
